---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Cours GitHub">
  <div class="bg-gradient-to-r from-blue-600 to-blue-800 text-white py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h1 class="text-4xl font-display font-bold mb-4">Cours GitHub</h1>
      <p class="text-xl text-blue-100">Guide complet pour maîtriser Git et GitHub</p>
    </div>
  </div>

  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    
    <!-- Section 1: Introduction -->
    <section class="mb-16" id="section-1">
      <h2 class="section-title">1. Qu'est-ce que GitHub ?</h2>
      
      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">1.1 Git vs GitHub</h3>
        <p class="text-gray-700 mb-4">
          Il est important de bien distinguer <strong>Git</strong> et <strong>GitHub</strong> :
        </p>
        <ul class="space-y-3 text-gray-700">
          <li class="flex items-start">
            <span class="font-bold text-blue-600 mr-2">Git :</span>
            <span>Un système de contrôle de version distribué créé en 2005 par Linus Torvalds. C'est un logiciel qui tourne sur votre ordinateur et qui permet de suivre les modifications de vos fichiers.</span>
          </li>
          <li class="flex items-start">
            <span class="font-bold text-green-600 mr-2">GitHub :</span>
            <span>Une plateforme web créée en 2008 qui héberge des dépôts Git en ligne. C'est comme un "réseau social pour développeurs" qui permet de collaborer et de partager du code.</span>
          </li>
        </ul>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">1.2 Pourquoi utiliser GitHub ?</h3>
        <div class="space-y-4 text-gray-700">
          <div>
            <h4 class="font-bold text-lg mb-2">🔄 Contrôle de version</h4>
            <p>Gardez un historique complet de toutes les modifications de votre code. Vous pouvez revenir en arrière à n'importe quel moment.</p>
          </div>
          <div>
            <h4 class="font-bold text-lg mb-2">👥 Collaboration</h4>
            <p>Travaillez en équipe sur le même projet sans risquer de perdre le travail des autres.</p>
          </div>
          <div>
            <h4 class="font-bold text-lg mb-2">💼 Portfolio professionnel</h4>
            <p>Montrez votre travail aux recruteurs. GitHub est devenu le CV des développeurs.</p>
          </div>
          <div>
            <h4 class="font-bold text-lg mb-2">🌍 Open Source</h4>
            <p>Participez à des projets mondiaux et apprenez des meilleurs développeurs.</p>
          </div>
        </div>
      </div>

      <div class="card bg-yellow-50 border-yellow-200">
        <h4 class="font-bold text-lg mb-3">✍️ Exercice 1 : Compréhension</h4>
        <p class="text-gray-700 mb-4">Expliquez avec vos propres mots la différence entre Git et GitHub :</p>
        <textarea 
          id="ex1" 
          class="input-field h-32 resize-none"
          placeholder="Écrivez votre réponse ici..."
        ></textarea>
      </div>
    </section>

    <!-- Section 2: Installation -->
    <section class="mb-16" id="section-2">
      <h2 class="section-title">2. Installation et configuration</h2>
      
      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">2.1 Installer Git</h3>
        <div class="space-y-4">
          <div>
            <h4 class="font-bold text-lg mb-2">Windows</h4>
            <ol class="list-decimal list-inside space-y-2 text-gray-700 ml-4">
              <li>Téléchargez Git depuis <a href="https://git-scm.com" target="_blank" class="text-blue-600 hover:underline">git-scm.com</a></li>
              <li>Lancez l'installateur et suivez les étapes (gardez les options par défaut)</li>
              <li>Vérifiez l'installation en ouvrant PowerShell et tapez : <code class="bg-gray-100 px-2 py-1 rounded">git --version</code></li>
            </ol>
          </div>
          <div>
            <h4 class="font-bold text-lg mb-2">macOS</h4>
            <p class="text-gray-700 mb-2">Ouvrez le Terminal et tapez :</p>
            <div class="code-block">brew install git</div>
          </div>
          <div>
            <h4 class="font-bold text-lg mb-2">Linux (Ubuntu/Debian)</h4>
            <div class="code-block">sudo apt-get install git</div>
          </div>
        </div>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">2.2 Configuration initiale</h3>
        <p class="text-gray-700 mb-4">Après l'installation, configurez votre identité :</p>
        <div class="code-block mb-4">
git config --global user.name "Votre Nom"<br/>
git config --global user.email "votre.email@example.com"
        </div>
        <p class="text-gray-700 mb-2">Vérifiez votre configuration :</p>
        <div class="code-block">
git config --list
        </div>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">2.3 Créer un compte GitHub</h3>
        <ol class="list-decimal list-inside space-y-2 text-gray-700 ml-4">
          <li>Allez sur <a href="https://github.com" target="_blank" class="text-blue-600 hover:underline">github.com</a></li>
          <li>Cliquez sur "Sign up" et créez votre compte</li>
          <li>Vérifiez votre email</li>
          <li>Complétez votre profil (photo, bio, etc.)</li>
        </ol>
      </div>

      <div class="card bg-yellow-50 border-yellow-200">
        <h4 class="font-bold text-lg mb-3">✍️ Exercice 2 : Configuration</h4>
        <p class="text-gray-700 mb-4">Notez les commandes que vous avez utilisées pour configurer Git :</p>
        <textarea 
          id="ex2" 
          class="input-field h-32 resize-none"
          placeholder="Collez ici les commandes de configuration..."
        ></textarea>
        <p class="text-gray-700 mt-4 mb-2">Quel est votre nom d'utilisateur GitHub ?</p>
        <input 
          type="text" 
          id="ex2-github" 
          class="input-field"
          placeholder="Votre pseudo GitHub"
        />
      </div>
    </section>

    <!-- Section 3: Commandes essentielles -->
    <section class="mb-16" id="section-3">
      <h2 class="section-title">3. Les commandes Git essentielles</h2>
      
      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">3.1 Initialiser un dépôt</h3>
        <p class="text-gray-700 mb-4">Pour transformer un dossier en dépôt Git :</p>
        <div class="code-block mb-4">
cd mon-projet<br/>
git init
        </div>
        <p class="text-gray-700">Cette commande crée un dossier caché <code class="bg-gray-100 px-2 py-1 rounded">.git</code> qui contient toute l'histoire de votre projet.</p>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">3.2 Vérifier l'état</h3>
        <p class="text-gray-700 mb-4">Pour voir les fichiers modifiés :</p>
        <div class="code-block">
git status
        </div>
        <p class="text-gray-700 mt-4">Cette commande vous montre :</p>
        <ul class="list-disc list-inside text-gray-700 mt-2 ml-4 space-y-1">
          <li>Les fichiers modifiés</li>
          <li>Les fichiers prêts à être committés</li>
          <li>Les fichiers non suivis</li>
        </ul>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">3.3 Ajouter des fichiers</h3>
        <p class="text-gray-700 mb-4">Pour préparer des fichiers à être committés :</p>
        <div class="code-block mb-4">
git add nom-fichier.txt          # Ajoute un fichier spécifique<br/>
git add .                         # Ajoute tous les fichiers modifiés
        </div>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">3.4 Créer un commit</h3>
        <p class="text-gray-700 mb-4">Un commit est comme une "photo" de votre projet à un instant T :</p>
        <div class="code-block">
git commit -m "Description des modifications"
        </div>
        <div class="bg-blue-50 border-l-4 border-blue-500 p-4 mt-4">
          <p class="text-sm text-gray-700">
            <strong>💡 Astuce :</strong> Écrivez des messages de commit clairs et descriptifs. 
            Au lieu de "changements", préférez "Ajout de la fonctionnalité de connexion utilisateur".
          </p>
        </div>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">3.5 Voir l'historique</h3>
        <div class="code-block mb-4">
git log                           # Historique complet<br/>
git log --oneline                 # Version simplifiée
        </div>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">3.6 Tableau récapitulatif</h3>
        <div class="overflow-x-auto">
          <table class="min-w-full bg-white border border-gray-300">
            <thead class="bg-gray-100">
              <tr>
                <th class="px-4 py-3 border-b text-left font-semibold">Commande</th>
                <th class="px-4 py-3 border-b text-left font-semibold">Description</th>
              </tr>
            </thead>
            <tbody class="text-gray-700">
              <tr>
                <td class="px-4 py-3 border-b font-mono text-sm">git init</td>
                <td class="px-4 py-3 border-b">Initialise un nouveau dépôt Git</td>
              </tr>
              <tr class="bg-gray-50">
                <td class="px-4 py-3 border-b font-mono text-sm">git status</td>
                <td class="px-4 py-3 border-b">Affiche l'état des fichiers</td>
              </tr>
              <tr>
                <td class="px-4 py-3 border-b font-mono text-sm">git add .</td>
                <td class="px-4 py-3 border-b">Ajoute tous les fichiers modifiés</td>
              </tr>
              <tr class="bg-gray-50">
                <td class="px-4 py-3 border-b font-mono text-sm">git commit -m "..."</td>
                <td class="px-4 py-3 border-b">Crée un commit avec un message</td>
              </tr>
              <tr>
                <td class="px-4 py-3 border-b font-mono text-sm">git log</td>
                <td class="px-4 py-3 border-b">Affiche l'historique des commits</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>

      <div class="card bg-yellow-50 border-yellow-200">
        <h4 class="font-bold text-lg mb-3">✍️ Exercice 3 : Pratique des commandes</h4>
        <p class="text-gray-700 mb-4">Quelle commande utiliseriez-vous pour :</p>
        <div class="space-y-4">
          <div>
            <label class="block text-gray-700 mb-2">a) Voir les fichiers modifiés ?</label>
            <input type="text" id="ex3a" class="input-field" placeholder="Tapez la commande..." />
          </div>
          <div>
            <label class="block text-gray-700 mb-2">b) Ajouter tous les fichiers ?</label>
            <input type="text" id="ex3b" class="input-field" placeholder="Tapez la commande..." />
          </div>
          <div>
            <label class="block text-gray-700 mb-2">c) Créer un commit ?</label>
            <input type="text" id="ex3c" class="input-field" placeholder="Tapez la commande..." />
          </div>
        </div>
      </div>
    </section>

    <!-- Section 4: Workflow GitHub -->
    <section class="mb-16" id="section-4">
      <h2 class="section-title">4. Travailler avec GitHub</h2>
      
      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">4.1 Créer un dépôt sur GitHub</h3>
        <ol class="list-decimal list-inside space-y-2 text-gray-700 ml-4">
          <li>Connectez-vous à GitHub</li>
          <li>Cliquez sur le bouton "+" en haut à droite, puis "New repository"</li>
          <li>Donnez un nom à votre dépôt (ex: "mon-premier-projet")</li>
          <li>Ajoutez une description (optionnel)</li>
          <li>Choisissez "Public" ou "Private"</li>
          <li>NE cochez PAS "Initialize with README" (on le fera localement)</li>
          <li>Cliquez sur "Create repository"</li>
        </ol>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">4.2 Lier votre dépôt local à GitHub</h3>
        <p class="text-gray-700 mb-4">Après avoir créé le dépôt sur GitHub, liez-le à votre projet local :</p>
        <div class="code-block">
git remote add origin https://github.com/votre-nom/mon-projet.git
        </div>
        <p class="text-gray-700 mt-4">Vérifiez la connexion :</p>
        <div class="code-block">
git remote -v
        </div>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">4.3 Pousser vos modifications</h3>
        <p class="text-gray-700 mb-4">Pour envoyer vos commits sur GitHub :</p>
        <div class="code-block mb-4">
git push -u origin main
        </div>
        <p class="text-gray-700">L'option <code class="bg-gray-100 px-2 py-1 rounded">-u</code> permet de définir la branche par défaut. Ensuite, vous pourrez utiliser simplement :</p>
        <div class="code-block">
git push
        </div>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">4.4 Récupérer les modifications</h3>
        <p class="text-gray-700 mb-4">Si vous travaillez sur plusieurs ordinateurs ou en équipe :</p>
        <div class="code-block">
git pull
        </div>
        <p class="text-gray-700 mt-4">Cette commande récupère et fusionne les dernières modifications depuis GitHub.</p>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">4.5 Cloner un dépôt existant</h3>
        <p class="text-gray-700 mb-4">Pour télécharger un projet depuis GitHub :</p>
        <div class="code-block">
git clone https://github.com/utilisateur/projet.git
        </div>
      </div>

      <div class="card bg-yellow-50 border-yellow-200">
        <h4 class="font-bold text-lg mb-3">✍️ Exercice 4 : Workflow complet</h4>
        <p class="text-gray-700 mb-4">Dans quel ordre effectueriez-vous ces étapes pour pousser un nouveau projet sur GitHub ? Numérotez de 1 à 5 :</p>
        <div class="space-y-2">
          <div class="flex items-center">
            <input type="number" id="ex4a" class="input-field w-16 mr-4" min="1" max="5" />
            <span class="text-gray-700">Créer un dépôt sur GitHub</span>
          </div>
          <div class="flex items-center">
            <input type="number" id="ex4b" class="input-field w-16 mr-4" min="1" max="5" />
            <span class="text-gray-700">Faire git init dans le dossier local</span>
          </div>
          <div class="flex items-center">
            <input type="number" id="ex4c" class="input-field w-16 mr-4" min="1" max="5" />
            <span class="text-gray-700">Faire git remote add origin</span>
          </div>
          <div class="flex items-center">
            <input type="number" id="ex4d" class="input-field w-16 mr-4" min="1" max="5" />
            <span class="text-gray-700">Faire git add . et git commit</span>
          </div>
          <div class="flex items-center">
            <input type="number" id="ex4e" class="input-field w-16 mr-4" min="1" max="5" />
            <span class="text-gray-700">Faire git push</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Section 5: Déploiement Netlify -->
    <section class="mb-16" id="section-5">
      <h2 class="section-title">5. Déployer sur Netlify</h2>
      
      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">5.1 Qu'est-ce que Netlify ?</h3>
        <p class="text-gray-700 mb-4">
          <strong>Netlify</strong> est une plateforme qui permet de déployer des sites web gratuitement et simplement. 
          Elle se connecte directement à votre dépôt GitHub et met à jour votre site automatiquement à chaque push !
        </p>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">5.2 Créer un compte Netlify</h3>
        <ol class="list-decimal list-inside space-y-2 text-gray-700 ml-4">
          <li>Allez sur <a href="https://www.netlify.com" target="_blank" class="text-blue-600 hover:underline">netlify.com</a></li>
          <li>Cliquez sur "Sign up"</li>
          <li>Choisissez "Sign up with GitHub" (c'est le plus simple)</li>
          <li>Autorisez Netlify à accéder à votre compte GitHub</li>
        </ol>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">5.3 Déployer votre site</h3>
        <ol class="list-decimal list-inside space-y-3 text-gray-700 ml-4">
          <li>
            <strong>Depuis Netlify :</strong> Cliquez sur "Add new site" → "Import an existing project"
          </li>
          <li>
            <strong>Connectez GitHub :</strong> Sélectionnez "GitHub" et autorisez l'accès
          </li>
          <li>
            <strong>Choisissez votre dépôt :</strong> Sélectionnez le projet que vous voulez déployer
          </li>
          <li>
            <strong>Configurez le build :</strong>
            <ul class="list-disc list-inside ml-6 mt-2 space-y-1">
              <li>Build command : <code class="bg-gray-100 px-2 py-1 rounded">npm run build</code></li>
              <li>Publish directory : <code class="bg-gray-100 px-2 py-1 rounded">dist</code></li>
            </ul>
          </li>
          <li>
            <strong>Déployez :</strong> Cliquez sur "Deploy site"
          </li>
        </ol>
        <div class="bg-green-50 border-l-4 border-green-500 p-4 mt-4">
          <p class="text-sm text-gray-700">
            <strong>🎉 Félicitations !</strong> Votre site est maintenant en ligne ! Netlify vous donne une URL du type : 
            <code class="bg-white px-2 py-1 rounded">random-name-123456.netlify.app</code>
          </p>
        </div>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">5.4 Personnaliser le nom de domaine</h3>
        <ol class="list-decimal list-inside space-y-2 text-gray-700 ml-4">
          <li>Dans Netlify, allez dans "Site settings"</li>
          <li>Cliquez sur "Change site name"</li>
          <li>Choisissez un nom disponible (ex: "mon-projet-ciel")</li>
          <li>Votre URL devient : <code class="bg-gray-100 px-2 py-1 rounded">mon-projet-ciel.netlify.app</code></li>
        </ol>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">5.5 Mises à jour automatiques</h3>
        <p class="text-gray-700 mb-4">
          Le plus magique : à chaque fois que vous faites <code class="bg-gray-100 px-2 py-1 rounded">git push</code>, 
          Netlify détecte les changements et redéploie automatiquement votre site ! 🚀
        </p>
      </div>

      <div class="card bg-yellow-50 border-yellow-200">
        <h4 class="font-bold text-lg mb-3">✍️ Exercice 5 : Déploiement</h4>
        <p class="text-gray-700 mb-4">Une fois que vous aurez déployé ce projet sur Netlify, notez l'URL de votre site :</p>
        <input 
          type="url" 
          id="ex5" 
          class="input-field"
          placeholder="https://mon-site.netlify.app"
        />
      </div>
    </section>

    <!-- Section 6: Bonnes pratiques -->
    <section class="mb-16" id="section-6">
      <h2 class="section-title">6. Bonnes pratiques</h2>
      
      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">6.1 Le fichier .gitignore</h3>
        <p class="text-gray-700 mb-4">
          Certains fichiers ne doivent PAS être envoyés sur GitHub (mots de passe, fichiers temporaires, etc.). 
          Le fichier <code class="bg-gray-100 px-2 py-1 rounded">.gitignore</code> permet de les exclure.
        </p>
        <p class="text-gray-700 mb-2">Exemple de <code class="bg-gray-100 px-2 py-1 rounded">.gitignore</code> :</p>
        <div class="code-block">
node_modules/<br/>
.env<br/>
*.log<br/>
dist/
        </div>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">6.2 Le fichier README.md</h3>
        <p class="text-gray-700 mb-4">
          Tout projet GitHub devrait avoir un <code class="bg-gray-100 px-2 py-1 rounded">README.md</code> qui explique :
        </p>
        <ul class="list-disc list-inside text-gray-700 space-y-2 ml-4">
          <li>Ce que fait le projet</li>
          <li>Comment l'installer</li>
          <li>Comment l'utiliser</li>
          <li>Qui a contribué</li>
        </ul>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">6.3 Messages de commit clairs</h3>
        <div class="space-y-3">
          <div>
            <p class="text-gray-700 mb-2">❌ Mauvais exemples :</p>
            <div class="code-block text-red-400">
git commit -m "fix"<br/>
git commit -m "changements"<br/>
git commit -m "update"
            </div>
          </div>
          <div>
            <p class="text-gray-700 mb-2">✅ Bons exemples :</p>
            <div class="code-block text-green-400">
git commit -m "Correction du bug d'affichage sur mobile"<br/>
git commit -m "Ajout de la page de contact"<br/>
git commit -m "Mise à jour de la documentation"
            </div>
          </div>
        </div>
      </div>

      <div class="card mb-6">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">6.4 Commits réguliers</h3>
        <p class="text-gray-700">
          Ne faites pas un énorme commit avec 50 fichiers modifiés. Préférez des petits commits réguliers et cohérents.
        </p>
      </div>
    </section>

    <!-- Bouton vers QCM -->
    <div class="text-center mt-16 mb-8">
      <div class="card bg-gradient-to-br from-green-50 to-blue-50 border-blue-200">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">🎯 Prêt pour le QCM ?</h3>
        <p class="text-lg text-gray-700 mb-6">
          Vous avez terminé le cours ! Il est temps de tester vos connaissances avec le QCM final de 20 questions.
        </p>
        <a href="/qcm" class="btn-primary inline-block text-lg px-8 py-4">
          Accéder au QCM Final →
        </a>
      </div>
    </div>

  </div>

  <script>
    // Sauvegarde automatique des réponses
    const inputs = document.querySelectorAll('input, textarea');
    
    inputs.forEach(input => {
      const id = input.id;
      
      // Charger la valeur sauvegardée
      const saved = localStorage.getItem(`cours_${id}`);
      if (saved) {
        input.value = saved;
      }
      
      // Sauvegarder à chaque modification
      input.addEventListener('input', () => {
        localStorage.setItem(`cours_${id}`, input.value);
      });
    });

    // Indicateur de sauvegarde
    let saveTimeout;
    inputs.forEach(input => {
      input.addEventListener('input', () => {
        clearTimeout(saveTimeout);
        showSaveIndicator();
        saveTimeout = setTimeout(hideSaveIndicator, 1500);
      });
    });

    function showSaveIndicator() {
      let indicator = document.getElementById('save-indicator');
      if (!indicator) {
        indicator = document.createElement('div');
        indicator.id = 'save-indicator';
        indicator.className = 'fixed bottom-4 right-4 bg-green-500 text-white px-4 py-2 rounded-lg shadow-lg z-50';
        indicator.textContent = '✓ Sauvegardé';
        document.body.appendChild(indicator);
      }
      indicator.style.display = 'block';
    }

    function hideSaveIndicator() {
      const indicator = document.getElementById('save-indicator');
      if (indicator) {
        indicator.style.display = 'none';
      }
    }
  </script>
</Layout>
